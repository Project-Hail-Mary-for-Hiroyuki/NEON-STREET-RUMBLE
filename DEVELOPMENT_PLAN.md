# DEVELOPMENT PLAN — 開発計画と完了チェックリスト

優先順の高いものから並べた開発計画です。チェックボックスは完了した項目です。

## 完了済み

### 基盤(既存実装)
- [x] クラスベースのゲーム構造 (Player / Enemy / Ninja / Robot / Projectile / HitEffect / Combo / Game)
- [x] 2.5D 座標系 (X左右 / Y奥行き / Zジャンプ) とベルトライン移動
- [x] プレイヤー: 左右移動・奥/手前移動・ジャンプ・3段コンボ・飛び蹴り
- [x] 敵: 忍者(近接+手裏剣投擲)・ロボット(重装備・チャージ攻撃)
- [x] コンボ・スコア・ゲームオーバー・リトライ
- [x] ネオン背景・雨・霧・CRT風エフェクト
- [x] PCキーボード操作
- [x] コードコピー / ダウンロード機能

### Phase 1: ビジュアル強化
- [x] 主人公: 青いボディ + 黒い輪郭 + 白ハイライト
- [x] 主人公: 攻撃時・ジャンプ時の残像
- [x] 主人公: 飛び蹴りの青いエネルギーエフェクト
- [x] 忍者: 黒服 + 赤く光る目 + 攻撃時の残像
- [x] ロボット: 大型化 + 銀/黒装甲プレート + 赤関節 + 赤目 + コアチャージ演出
- [x] ロボット: 爆発演出と画面揺れ

### タッチ操作(スマホ)
- [x] 仮想アナログレバー方式(4方向ボタンから刷新)
  - [x] タッチ座標の角度・距離から方向を計算
  - [x] 8方向(斜め同時入力)対応
  - [x] デッドゾーンによる誤操作防止
  - [x] ノブが倒した方向へ移動し、離すと滑らかに中央へ戻る
  - [x] 広いタッチ判定領域 + ポインターキャプチャで操作継続
- [x] マルチタッチ対応(レバー操作中に攻撃/ジャンプを別の指で同時操作可能)
- [x] レバーを離しても攻撃/ジャンプは解除されない(独立したPointer管理)
- [x] 左側レイアウト: ジャンプ(レバー上)・攻撃(レバー下)をすべて左手で操作
- [x] スマホ縦持ちの全画面化とセーフエリア対応
- [x] 右側に操作ボタンを置かない

### 2D化・操作整理（奥行きシステムの廃止）
2.5Dベルトスクロールの奥行き(ライン移動)を廃止し、純2Dの横スクロールとして単純化した。

- [x] 奥・手前のライン移動(Back/Front / W/S / 上/下)を撤去
- [x] `depthInfo()` を固定値(単一の地面線・固定スケール)に単純化し、キャラ/敵/武器/手裏剣/エフェクトのY依存を全て除去
- [x] 敵のY追従移動・Y距離判定を撤去し、当たり判定はX距離のみに統一
- [x] 特殊技(アッパーカットL / 突進I)をタッチUI・キー入力・タイトル説明から撤去
- [x] タッチUIを「ジャンプ / 攻撃 / 拾う / 武器切替」の4ボタンに整理
- [x] タイトル画面の操作説明に武器切替(Q)を明記
- [x] 奥行き依存の残存コード(LANE_ZS等)を修正し、全テスト(17項目)Pass
- [x] フル進行テスト追加: AREA1→AREA4→中ボス→CLEAR を自動検証(harness.js 8番)

## 次の優先順位(未着手)

### Phase 2a: プレイヤー新技（完了）

実装はデザイン側（別環境）で行い、Drive共有リンク経由で受け渡す。こちら側は取得・検証・コミットを担当する。

追加した技:
- [x] アッパーカット（キー L）: 上昇する拳。dmg 15 / リーチ 105 / dur 0.42s / ノックバック大。地上限定
- [x] 突進技（キー I）: 前方高速ダッシュで経路上の敵をまとめて命中。dmg 13 / リーチ 120 / dur 0.34s。地上限定

実装要件（デザイン側へ）:
- [x] PLAYER_ATTACKS 配列へ2エントリ追加（`uppercut` / `dashstrike`）。既存4エントリは変更しない
- [x] Input クラスに `special1Pressed` / `special2Pressed` を追加し、`KeyL` / `KeyI` で設定。`consumeFrame()`・`reset()` にも反映
- [x] Game.update() で `special1Pressed` / `special2Pressed` を検知して `player.startSpecial(0/1)` を呼ぶ
- [x] Player に `startSpecial(i)` を新設。地上時のみ発動。`dashstrike` は攻撃中 vx = facing * 480 相当の高速移動
- [x] スマホ: touch-ui に `data-action="special1"` / `"special2"` ボタンを攻撃・ジャンプの右側に追加。`bindTouchBtn` を拡張
- [x] タイトル画面の controls-grid に L / I の説明行を追記
- [x] 敵の打ち上げ物理は今回含めない（大きめのノックバックで代替）

検証結果:
- [x] 構文チェック（`node --check`）→ smoke2.js → lever.js が全て Pass
- [x] 新技テスト（`/tmp/opencode/special_test.js`）11項目 Pass: 地上発動・敵へのダメージ・前進距離・空中無効・攻撃中無効
- [x] スマホ実機確認（新ボタンのマルチタッチ）は未実施・要確認

### Stage 1: 4 AREA + 中ボス（最優先）
Stage 1の完成を最優先とする。既存の操作系・戦闘システム・スマートフォン対応・2.5Dベルトスクロール・世界観を維持し、大規模な変更を行う前に現在のコード構造を確認する。

#### Stage / Area構造
- [x] Stage 1のArea管理構造を実装
- [x] AREA 1 → AREA 2 → AREA 3 → AREA 4の順に進行
- [x] 各Areaの敵配置・開始位置・進行条件を管理
- [x] AREA 4突破後に中ボスを登場させる
- [x] 中ボス撃破でStage 1 CLEAR

#### Areaロックとスクロール
- [x] Area開始時に画面をロック
- [x] Area内の敵を全滅させるまで右方向へ進めない
- [x] 敵全滅時にロックを解除
- [x] ロック解除後に右方向へ進める
- [x] 現在の2.5Dベルトスクロールを維持したエリアスクロールを実装

#### 中ボス
- [x] Stage 1中ボスを実装
- [x] AREA 4クリアから中ボス戦へ自然に遷移
- [x] 中ボス撃破後にStage 1 CLEAR処理を実装

### Stage 1以降：武器システム
Stage 1のエリア進行基盤を壊さない形で武器システムを追加する。

#### 武器ドロップ
- [ ] 武器を持つ敵を実装
- [ ] 武器を持つ敵の撃破時に武器をドロップ
- [ ] ドロップする武器をランダム化
- [ ] 初期武器候補：バット・刀・ハンマー

#### 武器拾得・所持数
- [ ] プレイヤーがドロップ武器を拾えるようにする
- [ ] 武器種類ごとの所持数を管理
- [ ] 例：バット × 2 / 刀 × 3 / ハンマー × 1
- [ ] 拾った武器を実際の戦闘で使用できるようにする

#### 武器合成
- [ ] レシピ方式の武器合成システムを実装
- [ ] 武器種類と所持数から合成条件を判定
- [ ] 初期レシピ：刀 × 2 + バット × 2 → ヌンチャク × 1
- [ ] 合成によって必要な素材武器を消費
- [ ] 将来的な隠しレシピ追加に対応できる構造にする
- [ ] 将来的な武器コレクション機能に対応できる構造にする

### Stage 1以降：敵バリエーション
Stage 1完成を優先し、敵追加によって開発を不必要に遅らせない。

- [x] 通常ストリートファイター
- [x] 忍者（既存実装を活用）
- [x] ロボット（既存実装を活用）
- [ ] カンフー使い
- [ ] ドローン
- [x] 敵ごとの攻撃・移動・耐久力に個性を持たせる

### Stage 2以降：拡張候補
- [ ] 複数ステージ / 背景バリエーション
- [ ] ステージごとの難易度進行
- [ ] BGM・効果音の拡充
- [ ] 演出強化（シーン切り替え・演出カメラ等）
- [ ] 設定画面（サウンド・難易度等）

## 開発ルール

- 各 Phase 完了時: 構文チェック(`node --check`)→ 既存機能テスト → スマホ実機確認
- テストハーネス: `/tmp/opencode/smoke2.js`(ゲームロジック)・`/tmp/opencode/lever.js`(仮想レバー)・`/tmp/opencode/special_test.js`(新技)・`/tmp/opencode/persist_test.js`(ポーズ/永続化)
- DOM/実行時検証: `/tmp/opencode/jsdom_check.js`(jsdom で起動・ポーズ動作確認)
- コードは単一ファイル `index.html` に集約し、既存機能を壊さないこと
